this is for fresh minimal debian sett up will install i3 isntall.sh #puirple themed

make a isntall.sh that alignes to my color themed, and create a documentation  how this script works.

isolate per script and call it in a main script.
the script must:
1. update the pacakgae manager (apt)
2. setup a mirropr directory intend and reachable for philippines

3. isntall devlep tools 3. isntall this: 
$SUDO apt install -y \
  i3 xorg xinit lightdm lightdm-gtk-greeter \
  polybar picom rofi \
  stterm neovim firefox-esr \
  brightnessctl pulsemixer acpi \
  fonts-jetbrains-mono fonts-font-awesome \
  pulseaudio pavucontrol \
  git curl wget btop fzf zoxide eza \
  blueman network-manager-gnome \
  xss-lock feh yazi
5. create config files if not yet exist
6. apply color theme.
7. apply config files 
8. 
